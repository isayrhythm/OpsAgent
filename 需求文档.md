# **LangGraph Agent + Skill 执行设计文档**

## 1️⃣ 目标

构建一个 **可扩展、异步、用户友好**的 Agent 系统，支持：

- 用户请求自动路由到对应 Skill
- Skill 可以执行业务逻辑或生成/执行代码
- 执行结果 + 用户请求 + 历史交互交给 LLM 生成最终回复
- 提供实时进度反馈，用户无需长时间等待

------

## 2️⃣ 系统组件

### 2.1 主 Agent

- **职责**：
  - 接收用户请求
  - 根据任务意图选择 skill
  - 收集 skill 输出
  - 整合历史记录与用户输入，调用 LLM 生成最终回复
- **特点**：
  - 不关心 skill 内部执行细节
  - 支持异步调用子 agent 或 Worker
  - 可复用，不依赖 skill 具体实现

------

### 2.2 Skill

- **职责**：
  - 执行具体操作：查询数据、生成/执行代码、调用 API 等
  - 输出明确、结构化结果供 LLM 使用
- **实现方式**：
  1. **自动生成代码执行**：
     - Skill 定义输入/输出格式（JSON Schema）
     - LLM 根据输入生成 Python/SQL/其他代码
     - Skill 在安全环境执行生成代码，返回结果
  2. **直接封装执行逻辑**：
     - Skill 内部直接实现函数
     - 接受参数 → 返回结构化结果
- **输出规范**：
  - JSON/dict
  - 明确字段名和类型
  - 可包含多条记录列表
- **安全策略**：
  - 对自己写的 Skill，可信任源码
  - 对 LLM 自动生成的代码，建议做白名单限制（禁止 `os.system`, 文件删除等危险操作）
  - Skill 执行目录隔离

------

### 2.3 子 Agent / Task Worker

- **职责**：
  - 执行 skill（异步）
  - 监控执行状态
  - 通过 SSE 或队列反馈进度给前端
  - 执行完成，将结果回传主 Agent → LLM 整合
- **特点**：
  - 避免用户等待阻塞
  - 捕获异常、支持重试
  - 支持超时和任务取消
- **进度反馈示例**：

```json
{"step": 1, "status": "查询数据完成"}
{"step": 2, "status": "生成代码并执行中"}
{"step": 3, "status": "整合结果中"}
{"step": 4, "status": "完成", "result": {...}}
```

------

### 2.4 SSE（Server-Sent Events）

- **作用**：实时将 skill/子 agent 执行状态推送给前端
- **优势**：
  - 用户看到实时进度，不长时间等待
  - 可流式显示日志或进度条
  - 结合子 agent 完成异步执行
- **前端监听**

------

### 2.5 虚拟环境 / 安全隔离

暂时不用，后续再写

- **沙箱（可选）**：
  - 暂时先不用
- **风险折中**：
  - Skill 都是自己写的，可省略严格沙箱，仅用虚拟环境 + 目录隔离即可
  - 对 LLM 自动生成代码最好做白名单检查

------

## 3️⃣ 执行流程

1. **用户请求发送给主 Agent**
2. **主 Agent 路由到对应 Skill**
3. **Skill 在子 Agent / Worker 中异步执行**
   - 通过虚拟环境隔离依赖
   - 目录隔离执行
4. **子 Agent 通过 SSE 向前端实时推送进度**
5. **Skill 执行完成 → 返回结构化结果**
6. **主 Agent 收集结果 + 用户请求 + 历史交互**
7. **调用 LLM 整合生成最终回复**
8. **最终结果发送给用户**

------

### 4️⃣ 优势总结

- **模块化**：Agent 核心与 Skill 解耦
- **可扩展**：新增 Skill 无需修改 Agent
- **异步 + SSE**：用户体验流畅，实时反馈
- **高复用性**：主 Agent 核心逻辑稳定，Skill 独立管理

------

### 5️⃣ Codex 实现指南

- 按照以上模块结构编写代码：
  - **Agent**：路由、收集、调用 LLM
  - **Skill**：封装函数/代码执行逻辑，输入输出 JSON Schema
  - **子 Agent / Worker**：异步执行 skill，推送 SSE
  - **SSE**：提供实时状态给前端
  - **环境隔离**：虚拟环境 + 目录隔离
- 所有 Skill 输出必须可序列化
- LLM 负责最终整合，Skill 不直接生成回复文本



我已经创建了skill目录

底下有我现在的测试文件，可以供测试